const node = {
  name: 'figure',
  className: 'tagged-template-literals',
  style: { cssText: '' },
};

const styled = (styles, node) => {
  return null;
};

// curring function
const $styled = () => {
  return null;
}

/* 테스트 코드를 작성합니다. ----------------------------------------------------------- */

// styled 유틸리티

// $styled 유틸리티
